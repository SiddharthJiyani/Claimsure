{
  "name": "claimsure-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "concurrently \"npm run start:client\" \"npm run start:server\" \"npm run start:ai\"",
    "start:client": "cd client && pnpm dev",
    "start:server": "cd server && pnpm dev",
    "start:ai": "cd ai-server && source venv/bin/activate && python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload"
  },
  "dependencies": {
    "googleapis": "^180.0.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}